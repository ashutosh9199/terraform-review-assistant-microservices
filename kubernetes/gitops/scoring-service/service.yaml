apiVersion: v1
kind: Service
metadata:
  name: scoring-service
  namespace: production
  labels: { app: scoring-service }
spec:
  type: ClusterIP
  selector: { app: scoring-service }
  ports:
    - { name: http, port: 8005, targetPort: 8005 }
