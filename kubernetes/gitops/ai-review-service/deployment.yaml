apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-review-service
  namespace: production
  labels: { app: ai-review-service, app.kubernetes.io/part-of: terraform-review-assistant }
spec:
  replicas: 1
  selector:
    matchLabels: { app: ai-review-service }
  template:
    metadata:
      labels: { app: ai-review-service }
    spec:
      serviceAccountName: tra-app
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: ai-review-service
          image: traaksacrorhf1p.azurecr.io/ai-review-service:10ee779972eb7c3f3ace22b1778695af6bc43233
          ports: [{ containerPort: 8004 }]
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
          livenessProbe:
            httpGet: { path: /healthz, port: 8004 }
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /ready, port: 8004 }
            initialDelaySeconds: 5
            periodSeconds: 10
