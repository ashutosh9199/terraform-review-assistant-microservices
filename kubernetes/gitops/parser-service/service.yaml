apiVersion: v1
kind: Service
metadata:
  name: parser-service
  namespace: production
  labels: { app: parser-service }
spec:
  type: ClusterIP
  selector: { app: parser-service }
  ports:
    - { name: http, port: 8002, targetPort: 8002 }
